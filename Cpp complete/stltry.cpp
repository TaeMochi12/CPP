#include<bits/stdc++.h>
using namespace std ;
int main() {
    vector<string> arr(2);
    for(auto &it:arr)
    {
        cin>>it;
    }
    // sort(arr.begin(),arr.end());
    // reverse(arr.begin(),arr.end());
    // arr.push_back("Himanshi");
    // arr.push_back("1218");
    
    for(auto it:arr)
    {
        cout<<it<<endl;
    }




    return 0 ;
}